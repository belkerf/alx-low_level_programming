#include "main.h"
/**
 * mul - it multiple two integers
 * @a: the argument
 * @b: the socend argument
 * Return: its return the multiple
 */
int mul(int a, int b)
{
	int c;

	c = (a * b);
	return (c);
}
