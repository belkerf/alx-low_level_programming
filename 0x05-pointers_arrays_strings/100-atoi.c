#include "main.h"
/**
 * _atoi - function that convert a string to an integer.
 * Return : the integernwe convert.
 * @s : the string we want convert.
 */
int _atoi(char *s)
{
	/*the number in ascii 0 => 48 && 9=> 57*/
	int i = 0;

	while (s[i])
	{
		i++;
	}
	return (0);
}
